import requests
#GET REQUEST

# term = 'Madonna'
# res = requests.get(
#     'https://itunes.apple.com/search', params={'term': term, 'limit': 5})

# data = res.json()
# for result in data['results']:
#     # print(result['trackName'])
#     print(result['collectionName'])

# POST requests

data = {

    'username': 'd3nnysm2904',
    'tweets': [
        'hello', 'goodbye', {'id': 1, 'text': 'first tweet '}
    ]
}


requests.post(' t', json=data)
